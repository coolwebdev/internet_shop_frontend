import styled from 'styled-components'

const SpanInputGroup = styled.span`
  display: table-cell;
  width: 1%;
  white-space: nowrap;
  vertical-align: middle;
  position: relative;
  font-size: 0;
`

export default SpanInputGroup
