import styled from 'styled-components'

const CaptionFull = styled.div`
  padding: 9px;
  color: #333;

  h4 {
    font-size: 18px;
    margin-top: 10px;
    margin-bottom: 10px;
    font-family: inherit;
    font-weight: 500;
    line-height: 1.1;
    color: inherit;
  }

  p {
    margin: 0 0 10px;
  }
`

export default CaptionFull
